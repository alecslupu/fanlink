{
  activity: Rails.cache.fetch(["v5", "quest_activity", @req_source,  @quest_activity.id, @quest_activity.updated_at.to_i, @lang]) {
    render(partial: @req_source, locals: { activity: @quest_activity }, handlers: "jb")
  }
}

