json = {}

json[:recommended_people] = @people.map do |person|
  Rails.cache.fetch current_user.cache_key_follow_person("v5", @req_source, current_user, person) do
    render partial: "api/v5/people/#{@req_source}", locals: { person: person }, handlers: "jb"
  end
end

json
