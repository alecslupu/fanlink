{
  beacon: Rails.cache.fetch(["v5", "beacon", @req_source, @product_beacon.id, @product_beacon.updated_at]) {
    render(partial: @req_source, locals: { beacon: @product_beacon }, handlers: "jb")
  }
}
