{
  following: Rails.cache.fetch(["v5", "following", @req_source, @following.id, @following.updated_at.to_i]) {
    render(partial: @req_source, locals: { following: @following }, handlers: "jb")
  }
}

