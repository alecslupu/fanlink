json = {}

json[:types] = @activity_types.map do |activity|
  Rails.cache.fetch ["v5", "activity_type", @req_source, activity.id, activity.updated_at.to_i] do
    render partial: "activity", locals: { activity: activity }, handlers: "jb"
  end
end

json
