{
  lesson: Rails.cache.fetch(["v5", "lesson", @req_source, @lesson.id, @lesson.updated_at.to_i, @lang]) {
    render(partial: @req_source, locals: { lesson: @lesson, lang: @lang }, handlers: "jb")
  }
}

