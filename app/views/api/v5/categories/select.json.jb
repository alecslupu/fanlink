json = {}

json[:categories] = @categories.map do |category|
  Rails.cache.fetch ["v5", "category", "select", @req_source, category.id, category.updated_at] do
    {
      id: category.id,
      name: category.name,
    }
  end
end

json
