{
  completion: Rails.cache.fetch(["v5", "quest_completion", @req_source, @completion.id, @completion.updated_at.to_i]) {
    render(partial: @req_source, locals: { completion: @completion }, handlers: "jb")
  }
}
