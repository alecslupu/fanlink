json = {}

json[:categories] = @categories.map do |category|
  Rails.cache.fetch ["v4", @req_source, category.id, category.updated_at] do
    {
      id: category.id,
      name: category.name,
    }
  end
end

json
