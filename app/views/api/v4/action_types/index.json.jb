# frozen_string_literal: true
json = {}

json[:actions] = @action_types.map do |action|
  Rails.cache.fetch ["v4", "action", @req_source, action.id, action.updated_at.to_i] do
    render partial: @req_source, locals: { action: action }, handlers: "jb"
  end
end

json
