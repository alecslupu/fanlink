json = {}

json[:completions] = @completions.map do |completion|
  Rails.cache.fetch ["v4", @req_source, completion.id, completion.updated_at] do
    render partial: @req_source, locals: { completion: completion }, handlers: "jb"
  end
end

json
