{
  completion: Rails.cache.fetch(["v4", "quest_completion", @req_source, @completion.id, @completion.updated_at]) {
    render(partial: @req_source, locals: { completion: @completion }, handlers: "jb")
  }
}

