# frozen_string_literal: true

json = {
  id: prize.id,
  game_id: prize.trivia_game_id,
  position: prize.position,
  photo_url: AttachmentPresenter.new(prize.photo).url,
  description: prize.description
}

json
