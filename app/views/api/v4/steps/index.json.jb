# frozen_string_literal: true
json = {}

json[:steps] = @steps.map do |step|
  Rails.cache.fetch ["v4", "step", @req_source, step.id, step.updated_at.to_i] do
    render partial: @req_source, locals: { step: step }, handlers: "jb"
  end
end

json
