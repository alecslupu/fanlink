{
  interest: Rails.cache.fetch(["v4", "interests", @req_source, @interest.id, @interest.updated_at, @lang]) {
    render(partial: @req_source, locals: { interest: @interest, lang: @lang }, handlers: "jb")
  }
}
