# frozen_string_literal: true
{
  interest: Rails.cache.fetch(["v4", "interests", @req_source, @interest.id, @interest.updated_at.to_i, @lang]) {
    render(partial: @req_source, locals: { interest: @interest, lang: @lang }, handlers: "jb")
  }
}
