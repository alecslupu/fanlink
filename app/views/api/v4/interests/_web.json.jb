# frozen_string_literal: true
json = {
  id: interest.id,
  title: interest.title,
  order: interest.order,
}

if interest.children.present?
  json[:sub_interests] = interest.children.map do |child|
    render partial: @req_source, locals: { interest: child }, handler: "jb"
  end
else
  json[:sub_interests] = nil
end

json
