{
  message_report: Rails.cache.fetch(["v4", @message_report.id, @message_report.updated_at]) {
    render(partial: @req_source, locals: { message_report: @message_report, lang: @lang }, handlers: "jb")
  }
}
