json = Rails.cache.fetch ["v4", @message_report.id, @message_report.updated_at] do
  {
    message_report: render(partial: @req_source, locals: { message_report: @message_report }, handlers: "jb")
  }
end

json
