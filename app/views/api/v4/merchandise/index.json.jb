json = {}

json[:merchandise] = @merchandise.map do |merchandise|
  Rails.cache.fetch ["v4", @req_source, merchandise.id, merchandise.updated_at] do
    render partial: @req_source, locals: { merchandise: merchandise, lang: @lang }, handlers: "jb"
  end
end

json
