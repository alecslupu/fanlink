# frozen_string_literal: true
json = {}
json[:badge] = render partial: @req_source, locals: { badge: @badge }
if @req_source == "web"
  json[:badge][:assigned_rewards] = @badge.assigned_rewards
  json[:badge][:reward] = @badge.reward
end

json
