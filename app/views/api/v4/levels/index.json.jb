json = {}

json[:levels] = @levels.map do |level|
  Rails.cache.fetch ["v4", @req_source, level.id, level.updated_at, @lang] do
    render partial: @req_source, locals: { level: level, lang: @lang }, handlers: "jb"
  end
end

json
