# frozen_string_literal: true
{
  reward: Rails.cache.fetch(["v4", "reward", @req_source, @reward.id, @reward.updated_at.to_i, @lang]) {
    render(partial: @req_source, locals: { reward: @reward }, handlers: "jb")
  }
}
