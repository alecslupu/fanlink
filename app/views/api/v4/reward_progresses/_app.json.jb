# frozen_string_literal: true
{
  id: progress.id,
  reward_id: progress.reward_id,
  person_id: progress.person_id,
  series: progress.series,
  actions: progress.actions,
  total: @series_total || progress.total
}
