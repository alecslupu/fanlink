json = {}

json[:progresses] = @progresses.map do |progress|
  Rails.cache.fetch ["v4", "reward_progress", @req_source, progress.id, progress.updated_at] do
    render partial: @req_source, locals: { progress: progress }, handlers: "jb"
  end
end

json
