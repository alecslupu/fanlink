# frozen_string_literal: true
{
  progress: Rails.cache.fetch(["v4", "reward_progress", @req_source, @progress.id, @progress.updated_at.to_i]) {
    render(partial: @req_source, locals: { progress: @progress }, handlers: "jb")
  }
}
