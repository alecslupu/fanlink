json = Rails.cache.fetch ["v4", "comment", @req_source, @post_comment.id, @post_comment.updated_at] do
  {
    post_comment: render(partial: @req_source, locals: { post_comment: @post_comment }, handlers: "jb")
  }
end

json
