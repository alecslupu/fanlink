# frozen_string_literal: true
{
  event: Rails.cache.fetch(["v4", "event", @req_source, @event.id, @event.updated_at.to_i]) {
    render(partial: @req_source, locals: { event: @event, lang: @lang }, handlers: "jb")
  }
}
