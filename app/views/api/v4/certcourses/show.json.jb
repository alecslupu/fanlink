{
  certcourse: Rails.cache.fetch(["v4", "certcourse", @req_source, @certcourse.id, @certcourse.updated_at.to_i]) {
    render(partial: @req_source, locals: { certcourse: @certcourse }, handlers: "jb")
  }
}

