# frozen_string_literal: true

json = {
  id: following.id.to_s
}

json[:follower] = render partial: "api/v4/people/#{@req_source}", locals: { person: following.follower }, handler: 'jb'
json[:followed] = render partial: "api/v4/people/#{@req_source}", locals: { person: following.followed }, handler: 'jb'

json
