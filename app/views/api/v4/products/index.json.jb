json = {}

json[:products] = @products.map do |product|
  Rails.cache.fetch ["v4", @req_source, product.id, product.updated_at] do
    render partial: @req_source, locals: { product: product }, handlers: "jb"
  end
end

json
