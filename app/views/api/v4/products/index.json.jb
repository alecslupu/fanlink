# frozen_string_literal: true
# json = {}

# json[:products] = @products.map do |pd|
#   Rails.cache.fetch ["v4", "product", @req_source, pd.id, pd.updated_at] do
#     render partial: @req_source, locals: { pd: pd }, handlers: "jb"
#   end
# end

# json

{
  products: @products
}
