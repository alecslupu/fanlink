# frozen_string_literal: true
{
  assigned: Rails.cache.fetch(["v4", "assigned", @req_source, @assigned.id, @assigned.updated_at.to_i]) {
    render(partial: @req_source, locals: { assigned: @assigned }, handlers: "jb")
  }
}
