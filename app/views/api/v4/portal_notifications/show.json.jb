json = Rails.cache.fetch ["v4", "portal_notification", @req_source, @portal_notification.id, @portal_notification.updated_at] do
  {
    portal_notification: render(partial: @req_source, locals: { portal_notification: @portal_notification }, handlers: "jb")
  }
end

json
