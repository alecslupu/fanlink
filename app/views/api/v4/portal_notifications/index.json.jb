# frozen_string_literal: true

json = {}

json[:portal_notifications] = @portal_notifications.map do |portal_notification|
  render partial: @req_source, locals: { portal_notification: portal_notification }, handlers: 'jb'
end

json
