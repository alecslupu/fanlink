# frozen_string_literal: true
{
  message: Rails.cache.fetch(["v4", "message", @req_source, @message.id, @message.updated_at.to_i]) {
    render partial: @req_source, locals: { message: @message }, handlers: "jb"
  }
}
