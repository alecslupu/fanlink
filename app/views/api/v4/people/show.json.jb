# frozen_string_literal: true
{
  person: Rails.cache.fetch(["v4", "person", @req_source, @person.id, @person.updated_at.to_i]) {
    render(partial: @req_source, locals: { person: @person }, handlers: "jb")
  }
}
