# frozen_string_literal: true
json = {}
json[:person] = Rails.cache.fetch(["v4", "person", @req_source, @person.id, @person.updated_at.to_i]) {
  render(partial: "api/v4/people/#{@req_source}", locals: { person: @person }, handlers: "jb")
}
json[:person][:token] = @person.jwt_token
json
