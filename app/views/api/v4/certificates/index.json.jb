# frozen_string_literal: true

json = {}

json[:certificates] = @certificates.map do |certificate|
  render partial: @req_source, locals: { certificate: certificate, user: @current_user, pc: certificate.person_certificates }, handlers: "jb"
end

json
