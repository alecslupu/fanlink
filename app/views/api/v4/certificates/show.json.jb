# frozen_string_literal: true

json = {}
json[:certificate] = render(partial: @req_source, locals: { certificate: @certificate, user: @current_user, pc: @certificate.person_certificates }, handlers: 'jb')
json
