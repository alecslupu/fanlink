{
  certificate: Rails.cache.fetch(["v4", "certificate", @req_source, @certificate.id, @certificate.updated_at.to_i]) {
  	render(partial: "api/v4/certificates/#{@req_source}", locals: { certificate: @certificate, user: @current_user, pc: @certificate.person_certificates}, handlers: "jb")
  }
}

