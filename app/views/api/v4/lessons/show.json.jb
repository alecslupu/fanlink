{
  lesson: Rails.cache.fetch(["v4", "lesson", @req_source, @lesson.id, @lesson.updated_at, @lang]) {
    render(partial: @req_source, locals: { lesson: @lesson, lang: @lang }, handlers: "jb")
  }
}

