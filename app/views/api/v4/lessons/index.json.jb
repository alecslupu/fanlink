json = {}

json[:lessons] = @lessons.map do |lesson|
  Rails.cache.fetch ["v4", "lesson", @req_source, lesson.id, lesson.updated_at] do
    render partial: @req_source, locals: { lesson: lesson }, handlers: "jb"
  end
end

json
