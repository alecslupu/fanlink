# frozen_string_literal: true

{
  post: {
    id: @post.id,
    body: @post.body,
    picture_url: AttachmentPresenter.new(@post.picture).optimal_url,
    status: @post.status,
    person: {
      username: @post.person.username,
      picture_url: AttachmentPresenter.new(@post.person.picture).url.presence || @post.person.facebook_picture_url.presence
    }
  }
}
