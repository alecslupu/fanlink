json = {}

json[:course] = @courses.map do |course|
  Rails.cache.fetch ["v4", "course", @req_source, course.id, course.updated_at] do
    render partial: @req_source, locals: { course: course }, handlers: "jb"
  end
end

json
