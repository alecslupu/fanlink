json = {}
json[:certificates] = @certificates.map do |certificate|
  render partial: "api/v4/certificates/app", locals: { certificate: certificate, user: @current_user, pc: certificate.person_certificates }, handlers: "jb"
end

json
