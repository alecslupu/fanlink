json = {}

json[:relationships] = @relationships.map do |relationship|
  Rails.cache.fetch ["v4", @req_source, relationship.id, relationship.updated_at] do
    render partial: @req_source, locals: { relationship: relationship }, handlers: "jb"
  end
end

json
