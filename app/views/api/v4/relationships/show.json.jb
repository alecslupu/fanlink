{
  relationship: Rails.cache.fetch(["v4", "relationship", @req_source, @relationship.id, @relationship.updated_at]) {
    render(partial: @req_source, locals: { relationship: @relationship }, handlers: "jb")
  }
}

